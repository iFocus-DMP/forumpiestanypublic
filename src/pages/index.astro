---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import Section from '../components/Section.astro';
import Card from '../components/Card.astro';
import { project } from '../content/project';
---

<BaseLayout 
  title="Fórum Piešťany" 
  description="Občianska diskusná séria pred komunálnymi voľbami 2026 v Piešťanoch. Zvýšme účasť voličov a informovanosť občanov."
>
  <Hero 
    title="Fórum Piešťany" 
    subtitle="Občianska diskusná séria pred komunálnymi voľbami 2026"
  />
  
  <Section class="py-16 md:py-24">

    
    <!-- Bento Grid Layout -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6 auto-rows-fr">
      
      <!-- About Project (Large Item) -->
      <div class="md:col-span-2 bg-white rounded-3xl p-8 md:p-10 shadow-sm border border-gray-100 flex flex-col justify-center">
        <h2 class="text-3xl font-bold mb-6 text-gray-900">O projekte</h2>
        <p class="text-lg text-gray-600 leading-relaxed">
          Fórum Piešťany je séria diskusných eventov zameraných na kľúčové témy 
          ovplyvňujúce život v Piešťanoch. Projekt vytvára priestor pre informovanú 
          diskusiu medzi občanmi, expertmi a kandidátmi pred komunálnymi voľbami 2026.
        </p>
      </div>

      <!-- Primary Goal (Card) -->
      <Card title="Primárny cieľ" class="bg-primary-blue/5 border-primary-blue/10">
        <p class="text-gray-700 leading-relaxed">
          {project.goals.primary}
        </p>
      </Card>

      <!-- Secondary Goal (Card) -->
      <Card title="Sekundárny cieľ">
        <p class="text-gray-700 leading-relaxed">
          {project.goals.secondary}
        </p>
      </Card>

      <!-- Audience (Wide Card) -->
      <div class="md:col-span-2 bg-gradient-to-br from-gray-900 to-gray-800 rounded-3xl p-8 md:p-10 shadow-lg text-white">
        <h3 class="text-2xl font-bold mb-4">Komu je projekt určený</h3>
        <p class="text-gray-300 leading-relaxed text-lg">
          {project.audience}
        </p>
      </div>

    </div>

    <!-- Format Section -->
    <div class="mt-16">
      <div class="text-center mb-12">
        <h2 class="text-3xl font-bold mb-4">Formát eventov</h2>
        <p class="text-gray-600">Ako budú diskusie prebiehať</p>
      </div>
      
      <div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
        {[
          { icon: '⏱️', label: project.format.duration },
          { icon: '👥', label: project.format.audience },
          { icon: '📹', label: project.format.recording },
          { icon: '💬', label: project.format.questions },
          { icon: '📱', label: project.format.clips },
          { icon: '🇸🇰', label: project.format.language },
        ].map(item => (
          <div class="bg-white rounded-2xl p-6 text-center shadow-sm border border-gray-100 hover:shadow-md transition-shadow">
            <div class="text-3xl mb-3">{item.icon}</div>
            <p class="font-semibold text-sm text-gray-700">{item.label}</p>
          </div>
        ))}
      </div>
    </div>
    
    <!-- CTA Section -->
    <div class="mt-24 text-center">
      <a 
        href="/program" 
        class="inline-flex items-center justify-center bg-primary-blue text-white font-bold text-lg px-10 py-5 rounded-full hover:bg-primary-blue-600 hover:-translate-y-1 hover:shadow-lg transition-all duration-300"
      >
        Pozri si program 2026 
        <svg class="w-5 h-5 ml-2" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M14 5l7 7m0 0l-7 7m7-7H3"></path></svg>
      </a>
    </div>
  </Section>
</BaseLayout>

